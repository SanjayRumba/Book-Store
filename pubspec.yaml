name: book_shop
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.7 # Updated to a compatible version
  multiavatar: ^0.1.5
  font_awesome_flutter: ^10.5.0
  badges: ^2.0.1
  shared_preferences: ^2.2.0
  image_picker: ^1.0.1
  file_picker: ^5.3.3
  flutter_bloc: ^8.1.3
  cloud_firestore: ^4.8.3
  firebase_auth: ^4.7.1
  fluttertoast: ^8.2.2
  firebase_storage: ^11.2.5
  flutter_spinkit: ^5.2.0
  firebase_core: ^2.15.0
  qr_flutter: ^4.1.0
  qr_code_scanner: ^1.0.1
  scan: ^1.6.0
  crop: ^0.5.5
  smooth_page_indicator: ^1.1.0
  carousel_slider: ^4.2.1
  flutter_locales: ^3.0.1
  syncfusion_flutter_datepicker: ^21.2.4 # Updated to a compatible version
  date_format: ^2.0.7
  flare_flutter: ^3.0.2
  url_launcher: ^6.1.12
  khalti_flutter: ^3.0.0 # Updated to a version compatible with Flutter 2.x and null safety
  http: ^1.1.0
  dio: ^5.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
